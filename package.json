{
  "name": "dizrupt-tryon",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dizrupt Try-On — a console for the FASHN virtual try-on API. Zero dependencies.",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "test": "node test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
